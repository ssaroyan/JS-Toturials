# Products API (Node.js)

یک API ساده با Node.js که لیست محصولات را به‌صورت JSON برمی‌گرداند.

---

## 📁 ساختار پروژه

```
project/
├── data/
│   └── products.json
└── server.js
```

---

## 🚀 راه‌اندازی

```bash
node server.js
```

سرور روی پورت **3000** اجرا می‌شود.

---

## 📌 Endpoint ها

### GET `/api/products`

**توضیح:**  
لیست محصولات را به‌صورت JSON برمی‌گرداند.

**نمونه پاسخ:**

```json
[
  { "id": 1, "name": "Product 1" },
  { "id": 2, "name": "Product 2" }
]
```

---

## 🧠 توضیح کد

### ایمپورت ماژول‌ها

```js
const http = require("http");
const products = require("./data/products.json");
```

---

### ساخت سرور

```js
const server = http.createServer((req, res) => {
```

---

### بررسی Route

```js
if (req.url === "/api/products") {
```

⚠️ از `===` استفاده کنید، نه `=`.

---

### ارسال پاسخ JSON

```js
res.writeHead(200, {
  "Content-Type": "application/json"
});
res.end(JSON.stringify(products));
```

---

## ✅ کد کامل

```js
const http = require("http");
const products = require("./data/products.json");

const server = http.createServer((req, res) => {
  if (req.url === "/api/products") {
    res.writeHead(200, { "Content-Type": "application/json" });
    res.end(JSON.stringify(products));
  } else {
    res.writeHead(404, { "Content-Type": "text/plain" });
    res.end("Not Found");
  }
});

server.listen(3000, () => {
  console.log("Server running on port 3000");
});
```

---

## 🧪 تست API

```
http://localhost:3000/api/products
```

---

## 📌 نکات مهم

- `JSON.stringify` ضروری است
- بدون آن `res.write` خطا می‌دهد
- این پروژه بدون Express نوشته شده

---

## 📝 License

MIT
